This is a simple backend project to fetch Weather data from OpenWeather API keys.
ADD .env file also which includes: PORT, MON_URI, OPENWEATHER_API_KEYS.
At last do npm install to install dependencies.
